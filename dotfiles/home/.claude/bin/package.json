{
  "name": "claude-helpers",
  "private": true,
  "type": "module",
  "description": "Sources for pkgs.claude-helpers; the build vendors zod itself (see overlays/default.nix), so this is here for editors and ad-hoc `bun install`",
  "dependencies": {
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
