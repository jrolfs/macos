search_mode = "daemon-fuzzy"

auto_sync = true

style = "auto" ## auto, full, compact
show_preview = true

keymap_mode = "vim-insert" # auto, emacs, vim-insert, vim-normal
keymap_cursor = { emacs = "blink-block", vim_insert = "blink-bar", vim_normal = "steady-block" }

[daemon]
enabled = true
autostart = true

[ai]
enabled = true

[ai.opening]
send_cwd = true
send_last_command = true

#
# Paths

## where to store your database, default is your system data directory
## linux/mac: ~/.local/share/atuin/history.db
## windows: %USERPROFILE%/.local/share/atuin/history.db
# db_path = "~/.history.db"

## where to store your encryption key, default is your system data directory
## linux/mac: ~/.local/share/atuin/key
## windows: %USERPROFILE%/.local/share/atuin/key
# key_path = "~/.key"

## where to store your auth session token, default is your system data directory
## linux/mac: ~/.local/share/atuin/session
## windows: %USERPROFILE%/.local/share/atuin/session
# session_path = "~/.session"

#
# Interface

## date format used, either "us" or "uk"
# dialect = "us"

## the maximum number of lines the interface should take up
## set it to 0 to always go full screen
# inline_height = 0

#
# Sync

## address of the sync server
# sync_address = "https://api.atuin.sh"

## how often to sync history. note that this is only triggered when a command
## is ran, so sync intervals may well be longer
## set it to 0 to sync after every command
# sync_frequency = "1h"

#
# Search

## which search mode to use
## possible values: prefix, fulltext, fuzzy, skim
# search_mode = "fuzzy"

## which filter mode to use
## possible values: global, host, session, directory
# filter_mode = "global"

## which filter mode to use when atuin is invoked from a shell up-key binding
## the accepted values are identical to those of "filter_mode"
## leave unspecified to use same mode set in "filter_mode"
# filter_mode_shell_up_key_binding = "global"

## characters that count as a part of a word
# word_chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"

#
# Keyboard

## what to do when the escape key is pressed when searching
## possible values: return-original, return-query
# exit_mode = "return-original"

## possible values: emacs, subl
# word_jump_mode = "emacs"

## number of context lines to show when scrolling by pages
# scroll_context_lines = 1

## use ctrl instead of alt as the shortcut modifier key for numerical UI shortcuts
## alt-0 .. alt-9
# ctrl_n_shortcuts = false

#
# Secrets

## prevent commands matching any of these regexes from being written to history.
## Note that these regular expressions are unanchored, i.e. if they don't start
## with ^ or end with $, they'll match anywhere in the command.
## For details on the supported regular expression syntax, see
## https://docs.rs/regex/latest/regex/#syntax
# history_filter = [
#   "^secret-cmd",
#   "^innocuous-cmd .*--secret=.+"
# ]

## prevent commands run with cwd matching any of these regexes from being written
## to history. Note that these regular expressions are unanchored, i.e. if they don't
## start with ^ or end with $, they'll match anywhere in CWD.
## For details on the supported regular expression syntax, see
## https://docs.rs/regex/latest/regex/#syntax
# cwd_filter = [
#   "^/very/secret/area"
# ]

#
# AI

## the Atuin AI model alias to use for new sessions; run /model inside the AI
## interface to list the available ones
# model = ""

## minutes after the last interaction that a session is still auto-continued;
## use /new in the AI interface to force a fresh session
# session_continue_minutes = 60

## where AI sessions are stored, default is ai_sessions.db in the atuin data directory
# db_path = ""

## custom AI endpoint (e.g. a self-hosted atuin-ai-server) and its auth token;
## endpoint_protocol is one of "auto", "hub", "oss"
# endpoint = ""
# endpoint_protocol = "auto"
# api_token = ""

## skip ALL permission checks — use with caution
# yolo = false

## capabilities advertised to the LLM; all default to true, and when unset the
## AI still asks permission per use
# [ai.capabilities]
# enable_history_search = true
# enable_history_output = true
# enable_file_tools = true
# enable_command_execution = true

#
# Misc

## enable or disable automatic update checks
# update_check = true

